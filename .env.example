# Postgres
POSTGRES_USER=bookmarked
POSTGRES_PASSWORD=bookmarked
POSTGRES_DB=bookmarked

# Backend
DATABASE_URL=postgresql+psycopg2://bookmarked:bookmarked@db:5432/bookmarked
JWT_SECRET=change-me-in-production
CORS_ORIGINS=["*"]

# Flutter web app -> points at the backend from the browser's perspective
API_BASE_URL=http://localhost:8000
